<h1>Welcome <%= current_user.name %></h1>
<p>City: <%= current_user.wohnort %></p>
<ul>
<% @dashboard.user.hobbies.each do |hobby| %>
  <li><%= hobby.name %></li>
<% end %>
</ul>

 <h2><%= @similar_user.name if @similar_user != nil %></h2> %>

<%= link_to "Edit Profile", edit_dashboard_path(@dashboard) %>

<%= link_to "Sign Out", destroy_user_session_path, data: {turbo_method: :delete} %>
